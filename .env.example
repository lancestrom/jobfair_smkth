# Copy this file to .env in development/production and fill values.
# Do NOT commit your real .env file.

# Database (used by application/config/database.php)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=jobfair_smkth
DB_DRIVER=mysqli

# Example: on macOS with zsh you can export these in your shell or use a dotenv loader.
# export DB_HOST=127.0.0.1
# export DB_USER=myuser
# export DB_PASS=secret
# export DB_NAME=mydb